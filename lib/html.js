// require path and fs
const path = require('path');
const fs = require('fs');


// create path to templates
const tempDir = path.resolve(__dirname, '../template');


//create a function to push data to html
const generate = employees => {
    const html = [];
}

//html.push  and whatever the variable is 

//map to engineer, intern 